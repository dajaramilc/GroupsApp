apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-presence
  namespace: groupsapp
  labels: { app: svc-presence }
spec:
  replicas: 1
  selector:
    matchLabels: { app: svc-presence }
  template:
    metadata:
      labels: { app: svc-presence }
    spec:
      containers:
        - name: svc-presence
          image: dajaramilc/groupsapp-svc-presence:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8007
          envFrom:
            - secretRef: { name: app-secrets }
          resources:
            requests: { cpu: "50m", memory: "128Mi" }
            limits:   { cpu: "300m", memory: "256Mi" }
          readinessProbe:
            httpGet: { path: /health, port: 8007 }
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: svc-presence
  namespace: groupsapp
spec:
  selector: { app: svc-presence }
  ports:
    - port: 8007
      targetPort: 8007
